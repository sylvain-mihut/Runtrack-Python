# Écrire un programme qui trie dans l’ordre croissant une liste passés en paramètre.

def lenbis(liste):
    count = 0
    for i in liste:
        count += 1
    return count 

liste = [2, 8, 14, 65, 42, 4, 52]
